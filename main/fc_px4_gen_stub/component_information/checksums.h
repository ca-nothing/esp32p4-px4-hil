#pragma once
/*
 * checksums.h — stub for PX4's generated component_information/checksums.h. PX4's cmake produces the
 * component_general CRC; that generator isn't run here. COMPONENT_INFORMATION.hpp uses only
 * component_general_crc; stub=0 disables QGC metadata-CRC verification (feature off, still compiles).
 * ---
 * checksums.h — PX4'un uretilen component_information/checksums.h'i icin stub. PX4'un cmake'i
 * component_general CRC'sini uretir; o uretici burada calismaz. COMPONENT_INFORMATION.hpp yalnizca
 * component_general_crc kullanir; stub=0 QGC meta-veri-CRC dogrulamasini devre-disi birakir (ozellik kapali, yine de derlenir).
 */
#include <stdint.h>

namespace component_information
{
static constexpr uint32_t component_general_crc = 0;
}
